# nns-vesl configuration.
# Environment variables override these; CLI flags override everything.
#
# Sibling-repo paths (`nockchain`, `vesl-core`) are NOT configured
# here — they are hard-coded to `../nockchain` and `../vesl-core`
# relative to this repo in both `scripts/setup-hoon-tree.sh` and
# `Cargo.toml`. If your checkouts live elsewhere, symlink them at
# the expected paths instead of adding config.
#
# Hull HTTP API port (default: 3000)
# api_port = 3000

# --- Settlement mode ---
# v1 runs in local: kernel verifies, no chain interaction.
# Flip to "fakenet" or "dumbnet" to enable chain posting (requires chain_endpoint + key).
settlement_mode = "local"

# --- Chain settings (fakenet/dumbnet only) ---
# chain_endpoint = "https://localhost:5556"
# tx_fee = 256
# accept_timeout_secs = 300
